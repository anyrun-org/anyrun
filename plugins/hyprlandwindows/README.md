# Hyprland Windows

Focus a given window in Hyprland. Requires `hyprctl`.

## Usage

Just type in what window you are looking for.

## Configuration

Default config

```ron
Config(
  prefix: "",
)
```
