[package]
name = "clipboard"
version = "25.12.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
abi_stable = "0.11.1"
anyrun-plugin = { path = "../../anyrun-plugin" }
ron = "0.8.0"
serde = { version = "1.0.228", features = ["derive"] }
